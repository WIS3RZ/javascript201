// Part 14 : menggunakan matematika, selengkapnya hanya di developer.mozilla.org

const nilai = {
    x: 3,
    exp: 4,
    sqrt: 64,
    cbrt: 15625
};

console.log(Math.pow(nilai.x, nilai.exp));

console.log(Math.sqrt(nilai.sqrt));

console.log(Math.cbrt(nilai.cbrt));

